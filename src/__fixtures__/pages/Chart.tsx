import React, { memo } from "react"
import ChartComponent from "../chart"


function ChartDraw() {
    return (
        <>
            <ChartComponent />
        </>
    )
}

export default memo(ChartDraw)
